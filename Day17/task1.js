// Traverse ancestor in the DOM tree
let parentNode = document.getElementById('child').parentNode; // Get the parent node of the element with id 'child'
console.log(parentNode); // Output the parent node
console.log(parentNode.textContent); // Output the text content of the parent node

